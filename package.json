{
  "name": "server",
  "version": "1.0.0",
  "description": "For storing the data of Google Docs clone",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "client-install": "cd docs &&  npm install",
    "client-build": "cd docs && npm run build",
    "heroku-postbuild": "npm run client-install && npm run client-build"
  },
  "author": "Pradeep Joon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "http": "^0.0.1-security",
    "mongoose": "^7.3.3",
    "nodemon": "^3.0.1",
    "socket.io": "^4.7.1"
  }
}
